@{
    ViewData["Title"] = "Home Page";
}


@{
    ViewData["Title"] = "Start";
}

<h2>Välkommen till Connect Four!</h2>

@if (User.Identity?.IsAuthenticated ?? false)
{
    <p>Inloggad som <strong>@User.Identity!.Name</strong></p>

    <p>
        <form asp-controller="Game" asp-action="New" method="post" class="text-center d-inline">
            @Html.AntiForgeryToken()
            <button type="submit" class="btn btn-primary">Starta nytt spel</button>
        </form>
        <form asp-controller="Game" asp-action="Join" method="post" class="d-inline">
            @Html.AntiForgeryToken()
            <input type="number" name="id" placeholder="GameId" class="form-control d-inline-block" style="width:100px;" />
            <button type="submit" class="btn btn-secondary">Gå med i spel</button>
        </form>
        <form asp-controller="Login" asp-action="Logout" method="post" class="d-inline">
            <button type="submit" class="btn btn-danger">Logga ut</button>
        </form>
    </p>
}
else
{
    <p>
        <a class="btn btn-primary" asp-controller="Login" asp-action="Login">Logga in</a>
        <a class="btn btn-success" asp-controller="Login" asp-action="Register">Registrera</a>
    </p>
}
